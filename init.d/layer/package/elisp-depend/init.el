;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(require 'muki-lib)

(req 'elisp-depend
  (command (elisp-depend-insert-require)
           "elisp-depend"))

;;; init.el ends here
