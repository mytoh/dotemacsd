;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(use-package keyfreq
  :config
  (progn
    (keyfreq-mode 1)
    (keyfreq-autosave-mode 1)))


;;; init.el ends here
