;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(req 'escr
  (command (escr-frame-screenshot
            escr-region-screenshot
            escr-window-screenshot)
           "escr"))

;;; init.el ends here
