;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(liby 'digit-groups
  (add-hook 'after-init-hook
            (lambda () (require 'digit-groups))))

;;; init.el ends here
