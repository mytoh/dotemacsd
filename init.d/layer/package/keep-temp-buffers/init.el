;;; nitY -- nitY -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(liby 'keep-temp-buffers
  ;; (load-temp-buffers)
  ;; (add-hook 'kill-emacs-hook 'save-temp-buffers)
  )

;;; nit.elY ends here
