;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(use-package sidebar
  :commands (sidebar-open
             sidebar-buffers-open)
  ;; :config
  ;; (global-set-key (kbd "C-x C-f") 'sidebar-open)
  )

;;; init.el ends here
