;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(req 'evil-ReplaceWithRegister
  ;; change default key bindings (if you want) HERE
  ;; (setq evil-ReplaceWithRegister-key (kbd "gr"))
  (evil-ReplaceWithRegister-install))

;;; init.el ends here
