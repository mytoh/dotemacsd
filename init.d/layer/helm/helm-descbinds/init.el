;;; init -- init -*- lexical-binding: t; coding: utf-8; -*-

;;; Commentary:

;;; Code:

(req 'helm-descbinds
  (setq helm-descbinds-window-style 'split-window)
  (helm-descbinds-mode))

;;; init.el ends here
